Install vim CLI on windows
 
  install chocolatey
  choco install vim
  vim --file--
  i (insert)
  (press esc to exit editor mode)
 
  : (enter cmd mode)
  w (write)
  q (quit vim)
  q! (quit without writing)
